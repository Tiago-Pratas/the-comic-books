{
  "version": 2,
  "name": "the-comic-books-api",
  "builds": [
    { "src": "./app.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/", "dest": "/" },
    { "src": "/issues/(.*)", "dest": "/issues/(.*)" }
  ]
}